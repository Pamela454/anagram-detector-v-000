# Your code goes here!
class Anagram
  attr_accessor :name

  def initialize(word)
    @name = word
  end

  def match(word) #take in an array and return all matches in an array
    word.collect {|w| w == }
  else []
  end
end
